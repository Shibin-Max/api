#!/usr/bin/env groovy
@Library('library') _

dockerBuildSCM_v7(
    'target/c66-game-betslip-check-api.jar', // 此处填写打包最终生成文件的相对路径
    'product',  // 此处填写镜像仓库的模块组名
    'c66-game-betslip-check-api') // 此处填写与运维商定好的模块名


